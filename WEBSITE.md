# Website plan — `https://kimmo.cloud/pintheft/`

Publish the PinTheft tracker as a single-page static site, generated by
**Hugo** locally and rsync'd to a personal nginx server (`haig`).  The
build infrastructure mirrors the sibling CVE-2026-43284 (Dirty Frag)
tracker; only the content and a few config strings differ.

## Architecture

- **Source repo:** `github.com/suominen/pintheft` (this repo).
- **Generator:** Hugo extended, ≥ 0.146.0.  Run locally; nothing built
  in CI.
- **Source layout:** Hugo project under `site/`.  The tracker is a
  single page at `site/content/_index.md`.
- **Theme:** PaperMod, integrated as a Hugo Module (no submodule).  See
  `site/hugo.toml` and `site/go.mod`.
- **Dev environment:** Nix flake (`flake.nix`) provides hugo + go + git.
  Auto-activates via `.envrc` if direnv is installed.
- **Build:** `make build` → `hugo --minify --gc --cleanDestinationDir`,
  output in `site/public/`.
- **Publish:** `make dist` → `rsync -avz --delete site/public/`
  → `haig:.www/sites/kimmo.cloud/htdocs/pintheft/`.
- **Web server:** existing nginx vhost on `kimmo.cloud` serves
  `htdocs/pintheft/` directly at the URL path `/pintheft/` (directory
  name matches URL path; no `alias` directive needed).  TLS, redirects,
  and certbot are managed by the host, not by this repo.
- **DNS:** unchanged.  `kimmo.cloud` already resolves to the haig
  server.

## Naming — pre-CVE slug

No CVE has been assigned to PinTheft yet.  Following the precedent of
the Dirty Frag tracker (originally published under `/dirty-frag/`, then
renamed to `/CVE-2026-43284/` once MITRE assigned the ID), this site is
published under its nickname slug `/pintheft/` for now:

- repo: `~/src/pintheft`, `github.com/suominen/pintheft`
- Go module path: `github.com/suominen/pintheft/site`
- URL: `https://kimmo.cloud/pintheft/`
- systemd units: `pintheft-tracker-update.{service,timer}`

When a CVE is assigned, retarget `baseURL`, the repo/dir name, the Go
module path, and the rsync destination to the CVE ID — and leave (or
add) a redirect from `/pintheft/` so existing links survive.

## Steps

### 1. Hugo project — scaffolded from CVE-2026-43284

The Hugo skeleton, theme integration, PaperMod overrides, CSS, and i18n
were copied wholesale from the CVE-2026-43284 (Dirty Frag) tracker on
2026-05-20.  Only `baseURL`, `title`, the Go module path, and the rsync
destination were retargeted.

### 2. Tracker content — initial draft

- [x] `site/content/_index.md` authored from the upstream PinTheft PoC
      (V12 `v12-security/pocs` README + `poc.c` header), the
      oss-security disclosure thread, and direct kernel-tree inspection
      of the fix commits.
- [x] Single-CVE structure — one bug, one component (unlike Dirty Frag's
      two-variant breakdown).
- [x] First-pass distro entries — Debian, Proxmox, NixOS, Rocky,
      Amazon Linux, Arch, and Fedora.  Rocky 8/9/10 not affected
      (`# CONFIG_RDS is not set`); Amazon Linux left unverified.
- [x] Social/OpenGraph banner — `site/assets/pintheft-tracker.svg`
      rasterised to `site/static/pintheft-tracker.png` by `make banner`
      (resvg), wired into the `cover:` front-matter.

### 3. Local dev — done

- [x] `flake.nix` provides hugo + go + git.
- [x] `.envrc` for direnv auto-activation (`direnv allow` once).
- [x] `README.md` documents the workflow.

### 4. Publish — pending

- [ ] First `make build` and inspect `site/public/` locally.
- [ ] First `make dist` to push to `haig` — `htdocs/pintheft/` created
      (let rsync make it).
- [ ] Verify the site renders at `https://kimmo.cloud/pintheft/`,
      including the RSS feed and OG metadata.

### 5. Automated maintenance — pending one-time setup

- [ ] `git worktree add -b auto-update ~/src/auto-update/pintheft main`
- [ ] Install and enable `systemd/pintheft-tracker-update.{service,timer}`
      as user units.

## Decisions

- **Hosting:** own nginx on `haig`, *not* GitHub Pages.  Same as the
  sibling trackers.
- **URL:** `https://kimmo.cloud/pintheft/` (nickname slug; see "Naming"
  above).
- **Theme integration:** Hugo Modules.
- **Canonical source:** `site/content/_index.md`.
- **Theme:** PaperMod.
- **Automated maintenance:** a user-level systemd timer
  (`systemd/pintheft-tracker-update.timer`, twice daily) runs
  `scripts/auto-update`, which merges `origin/main` into a dedicated
  long-lived `auto-update` branch in a separate worktree and hands off
  to headless Claude with `scripts/auto-update-prompt.txt`.  The agent
  only commits onto `auto-update` — it does not push or open PRs.
  Merges of `auto-update` into `main` are done manually.

## Risks & gotchas

- **Anchor drift.** Hugo/Goldmark's heading slugger differs from
  GitHub's for headings with em dashes, parens, plus signs, or slashes.
  Pin brittle headings with `### Heading {#stable-id}` if external
  bookmarks need to survive renames.
- **Subpath baseURL.** `baseURL = "https://kimmo.cloud/pintheft/"` must
  include the trailing slash and the path component.  Hugo uses this for
  all internal links.
- **`.cleanDestinationDir`** wipes `site/public/` before each build, so
  don't put hand-edited files there.
- **rsync `--delete`** removes server-side files not present in the
  build output.  Don't store unrelated content under
  `htdocs/pintheft/` — `make dist` will erase it.
- **Client-side table compaction.** `layouts/partials/extend_footer.html`
  rewrites tables in the browser to collapse consecutive duplicate
  first-column cells via `rowSpan`.  The Markdown source still has those
  cells; only the rendered DOM is compacted.

## Social banner

The OpenGraph / social-preview image is generated from an SVG source:
`site/assets/pintheft-tracker.svg` (1200×630) is rasterised to
`site/static/pintheft-tracker.png` by `make banner`, which runs `resvg`.
The PNG is committed so ordinary `make build` / `make dist` runs need no
rasteriser; `make banner` is only run after editing the SVG.  It needs
`resvg` (in the Nix flake) and the Roboto fonts (`fonts-roboto-unhinted`
on Debian) — without Roboto, resvg silently drops the SVG's sans-serif
text.  SVG was chosen as the source because social crawlers only accept
raster `og:image`, and Hugo's image pipeline cannot process SVG.

## Known gaps

- [ ] **Favicons.** PaperMod's `head.html` emits five icon `<link>` tags
      that 404 unless the icon files exist in `site/static/`.  The
      sibling trackers have the same open item.

## Out of scope

- Search and multi-page navigation.  Hugo + PaperMod can do these later
  if needed.  (RSS and the dark/light toggle are already wired up.)
